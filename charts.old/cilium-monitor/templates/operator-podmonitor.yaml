{{- if .Values.enablePodMonitor.operator -}}
apiVersion: monitoring.coreos.com/v1
kind: PodMonitor
metadata:
  name: cilium-operator-metrics
  labels:
    name: cilium-operator
spec:
  selector:
    matchLabels:
      name: cilium-operator
  podMetricsEndpoints:
  - port: prometheus
{{- end }}
