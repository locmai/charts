apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "dashy.fullname" . }}
  labels:
    {{- include "dashy.labels" . | nindent 4 }}
data:
  conf.yml: |
    {{- toYaml .Values.config | nindent 4 }}
