apiVersion: v1
kind: Service
metadata:
  name: {{ include "matrixalertmanager.fullname" . }}
  labels:
    {{- include "matrixalertmanager.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "matrixalertmanager.selectorLabels" . | nindent 4 }}
