apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "matrixalertmanager.fullname" . }}-test-connection"
  labels:
    {{- include "matrixalertmanager.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "matrixalertmanager.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
