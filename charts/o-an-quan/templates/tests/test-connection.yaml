apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "o-an-quan.fullname" . }}-test-connection"
  labels:
    {{- include "o-an-quan.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "o-an-quan.fullname" . }}:{{ .Values.backend.service.port }}']
  restartPolicy: Never
